const styles = theme => ({
  root: {
    width: '100%',
    height: '100%',
    display: 'flex',
    justifyContent: 'center',
    alignItems: 'center',
    alignContent: 'center',
  },
  list: {
    maxWidth: '400px',
    backgroundColor: theme.palette.background.paper,
  },
});

export default styles;
